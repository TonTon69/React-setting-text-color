import React, { Component } from "react";

class Reset extends Component {
  render() {
    return (
      <button type="button" className="btn btn-primary float-left mt-4">
        Reset
      </button>
    );
  }
}

export default Reset;
